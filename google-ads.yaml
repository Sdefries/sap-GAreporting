# google-ads.yaml
# Place this file at the project root — never commit to GitHub
# Get credentials from: console.cloud.google.com → APIs → Google Ads API

developer_token: YOUR_DEVELOPER_TOKEN
client_id: YOUR_OAUTH_CLIENT_ID
client_secret: YOUR_OAUTH_CLIENT_SECRET
refresh_token: YOUR_REFRESH_TOKEN
login_customer_id: YOUR_MCC_CUSTOMER_ID   # Your manager account ID (no dashes)
use_proto_plus: True
